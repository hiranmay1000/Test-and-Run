#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int minDeletionSize(vector<string>& strs) {
      for(auto it: strs[0].size()){
        for(auto jt: strs.size()){
            if(strs[it][jt]<strs[])
        }
      }
    }
};

int main()
{
    Solution problem;

    vector<string> s ={"abc", "bac", "bee"};
    cout << "Column: " << problem.minDeletionSize(s) << endl;
}
